function push(list,items){Array.prototype.push.apply(list,items);list.length_=list.length;list.length_+=items.length}
const focusableContent=[];const focusableElements='button, [href], input, select, span, textarea, [tabindex]:not([tabindex="-1"])';const menu=document.querySelector('#overlay-menu');const toogleButton=document.querySelectorAll('#menu-toggle');const focusableItems=menu.querySelectorAll(focusableElements);focusableContent.push.apply(focusableContent,toogleButton);focusableContent.push.apply(focusableContent,focusableItems);const firstFocusableElement=focusableContent[0];const lastFocusableElement=focusableContent[focusableContent.length-1];document.addEventListener('keydown',function(e){let isTabPressed=e.key==='Tab'||e.keyCode===9;if(!isTabPressed||!menu.classList.contains('open')){return}
if(e.shiftKey){if(document.activeElement===firstFocusableElement){lastFocusableElement.focus();e.preventDefault()}}else{if(document.activeElement===lastFocusableElement){firstFocusableElement.focus();e.preventDefault()}}});(function($){$(document).ready(function(){$(focusableElements,$('#overlay-menu')).each(function(){$(this).attr('tabindex','-1')});$('#menu-toggle').on('click',function(){if($('.site-branding-overlay .navbar-logo').length==0){$('.site-branding-overlay').html($('.site-branding').html())}
$(this).toggleClass('open');$('#'+$(this).data('id')).toggleClass('open');if($('#overlay-menu').hasClass('open')){$('body').addClass('fullpage-open')
if($(window).width()>=1200){$('input[type=search]').focus()}
$(focusableElements,$('#overlay-menu.open')).each(function(){$(this).attr('tabindex','0')})}else{$(focusableElements,$('#overlay-menu')).each(function(){$('body').removeClass('fullpage-open');$(this).attr('tabindex','-1')})}});$(document).keydown(function(e){if(e.keyCode===27&&$('#overlay-menu').hasClass('open')){$('body').removeClass('fullpage-open');$('#menu-toggle').removeClass('open');$('#overlay-menu').removeClass('open');$(focusableElements,$('#overlay-menu')).each(function(){$(this).attr('tabindex','-1')})}})})})(jQuery)